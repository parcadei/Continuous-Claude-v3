# Continuous Claude Environment Variables
# Copy to .env and customize as needed

# PostgreSQL Connection
# Default: Uses Docker container on localhost
# For remote database, set OPC_POSTGRES_URL:
# OPC_POSTGRES_URL=postgresql://user:password@hostname:5432/continuous_claude

# Alternative: Standard DATABASE_URL (used by some scripts)
# DATABASE_URL=postgresql://user:password@hostname:5432/continuous_claude

# Docker PostgreSQL defaults (used by docker-compose.yml)
POSTGRES_USER=claude
POSTGRES_PASSWORD=claude_dev
POSTGRES_DB=continuous_claude
POSTGRES_PORT=5432

# Optional API Keys (not required for core functionality)
# BRAINTRUST_API_KEY=your_braintrust_key_here
# PERPLEXITY_API_KEY=your_perplexity_key_here
# NIA_API_KEY=your_nia_key_here
